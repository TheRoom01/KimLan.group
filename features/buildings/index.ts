/**
 * Public entry point for building-domain UI, queries and services.
 * Existing routes remain in app/owner/properties while the feature is migrated incrementally.
 */
export const BUILDINGS_FEATURE = "buildings";

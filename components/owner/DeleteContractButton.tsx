"use client";
import { Trash2,X } from "lucide-react";import { useRouter } from "next/navigation";import { useState } from "react";import { createPortal } from "react-dom";import { readApiResponse } from "@/lib/api/client";
export default function DeleteContractButton({contractId}:{contractId:string}){const router=useRouter();const[open,setOpen]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState<string|null>(null);async function remove(){setLoading(true);setError(null);try{await readApiResponse(await fetch(`/api/owner/contracts/${contractId}`,{method:"DELETE"}));setOpen(false);router.replace("/owner/contracts")}catch(e){setError(e instanceof Error?e.message:"Không thể chuyển hợp đồng vào thùng rác");setLoading(false)}}return <><button type="button" onClick={()=>setOpen(true)} className="inline-flex items-center gap-2 rounded-xl border border-red-200 bg-red-50 px-4 py-2 text-sm font-semibold text-red-700 hover:bg-red-100"><Trash2 size={16}/>Xóa</button>{open&&typeof document!=="undefined"?createPortal(<div className="fixed inset-0 z-[9998] grid place-items-center bg-black/55 p-4 backdrop-blur-sm"><div className="w-full max-w-sm rounded-3xl bg-[#fff9ef] p-5 shadow-2xl"><div className="flex justify-between gap-3"><div><h2 className="font-black text-[#432918]">Chuyển hợp đồng vào thùng rác?</h2><p className="mt-2 text-sm leading-6 text-[#80634a]">Hợp đồng sẽ được giữ 20 ngày và có thể khôi phục trong thời gian này.</p></div><button aria-label="Đóng" disabled={loading} onClick={()=>setOpen(false)}><X size={19}/></button></div>{error?<p className="mt-3 text-sm text-red-700">{error}</p>:null}<div className="mt-5 flex justify-end gap-2"><button disabled={loading} onClick={()=>setOpen(false)} className="rounded-xl border px-4 py-2 text-sm font-semibold">Hủy</button><button disabled={loading} onClick={()=>void remove()} className="rounded-xl bg-red-700 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{loading?"Đang chuyển...":"Chuyển vào thùng rác"}</button></div></div></div>,document.body):null}</>}
